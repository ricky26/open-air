//! A module for interfacing with the protocols used by the Aurora ATC client.
//! Primarily this is static data (like sector files), since the other protocols
//! are shared between clients.

pub mod gdf;
pub mod sector;

